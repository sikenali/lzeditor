# LZMail 整体设计思路

> 生成时间：2026-09-16  
> 项目名称：懒猫微邮（LZMail）—— 自托管多账号邮件客户端

---

## 一、设计理念

**温暖、东方、克制**。

LZMail 整体风格定位为「中式现代主义」：以米色/暖棕为基底，朱红作为强调色，金色作为点缀色，融入 serif 衬线字体带来的人文气息，同时保持界面干净、层次清晰的功能性。避免冷冰冰的科技感，营造「个人邮箱助理」的亲近感。

核心原则：
- **低对比度护眼**：背景不使用纯白/纯黑，而是带暖调的米色与深棕
- **衬线体传递质感**：标题用书法字体，正文用宋体，形成层次区分
- **功能优先，装饰适度**：动画存在但不喧宾夺主，开关可随时关闭
- **品牌色服务语义**：朱红=危险/重要，金色=价值/统计，绿色=成功/同步，青色=联系人

---

## 二、色系系统

### 2.1 基础配色

| 角色 | Light 值 | Dark 值 | 语义 |
|------|----------|---------|------|
| 背景 | `#fbf7f0` 暖米白 | `#1a1612` 深暖棕 | 页面底色 |
| 卡片 | `#ffffff` | `#2a2320` 暖灰棕 | 内容容器 |
| 边框 | `#f3ede3` | `#3d3430` | 分隔线 |
| 主文字 | `#3d2b1f` 深棕 | `#f5f0e8` 米白 | 正文 |
| 次文字 | `#6b5b4f` 中棕 | `#d4c8b8` | 辅助说明 |
| 弱文字 | `#8b7355` 浅棕 | `#a09080` | 时间/标签 |

### 2.2 语义色

| 角色 | Light 值 | Dark 值 | 用途 |
|------|----------|---------|------|
| 主色（朱红） | `#c43d3d` | `#d44638` | 强调按钮、重要操作、未读标记 |
| 成功（玉绿） | `#5b8c5a` | `#6b9b6a` | 同步正常、发送成功 |
| 警告（金色） | `#c9a96e` | `#d4b87a` | 统计高亮、稍后处理、进度条 |
| 信息（云蓝） | `#6b8fa3` | `#6b8fa3` | 联系人、中性提示 |
| 危险（深红） | `#c43d3d` | `#d44638` | 删除、错误 |

### 2.3 服务商品牌色

每个邮箱提供商有独立标识色，用于侧边栏头像：

| 服务商 | 色值 |
|--------|------|
| Gmail | `#ea4335` |
| Outlook | `#0078d4` |
| QQ Mail | `#12b7f5` |
| 网易邮箱 | `#e53e3e` |
| iCloud | `#007aff`（默认 fallback `#6366f1`） |
| Yahoo | `#720e9e`（默认 fallback `#6366f1`） |
| 其他 | `#6366f1` |

### 2.4 主题色可选色板

用户可在设置中切换整体主题色调：
- 朱红（默认）：`#c43d3d`
- 云蓝：`#6b8fa3`
- 玉绿：`#5b8c5a`
- 金色：`#c9a96e`
- 墨色：（自定义扩展）

---

## 三、字体系统

### 3.1 字体选择

| 用途 | 字体 | 字重 | 背景 |
|------|------|------|------|
| 正文 | `Noto Serif SC`（思源宋体） | 400/500 | 跨平台中文宋体，现代适配 |
| 标题/Logo | `Ma Shan Zheng`（马善政毛笔楷体） | 400 | 书法感，营造东方气质 |
| 装饰标题 | `ZCOOL XiaoWei`（站酷小薇体） | 400 | 柔和手写感，用于点缀 |
| 代码/等宽 | `SF Mono`, Monaco | 400 | 编辑器内联代码 |

### 3.2 字号层级

| 层级 | Light | Dark | 用途 |
|------|-------|------|------|
| H1（页面大标题） | 28px | 28px | 页面头部，如"仪表盘" |
| H2（板块标题） | 22px | 22px | 邮件主题、卡片标题 |
| H3（小标题） | 18px | 18px | 面板标题 |
| 正文 | 14px / 16px / 18px | 同上 | 可设 小/中/大 三档 |
| 辅助文字 | 12px | 12px | 时间戳、标签 |
| 按钮文字 | 13px | 13px | 导航、Tab |

### 3.3 字体加载策略

`<head>` 中预加载 Google Fonts，使用 `font-display: swap` 避免闪烁，CSS 变量控制字体大小档位。

---

## 四、背景与空间设计

### 4.1 背景体系

- **应用背景**：暖米色（Light）/ 深暖棕（Dark），非纯白/纯黑，降低视觉疲劳
- **卡片背景**：纯白（Light）/ 暖灰棕（Dark），通过轻微阴影（`0 2px 8px rgba(0,0,0,0.04)`）与背景分离
- **侧边栏背景**：与应用背景同色，通过边框线区分
- **模态遮罩**：`rgba(0,0,0,0.5)` 半透明黑，`backdrop-blur-[2px]` 轻微毛玻璃

### 4.2 圆角体系

| 元素 | 圆角 |
|------|------|
| 卡片/弹窗 | `16px`（`rounded-[16px]`） |
| 按钮/Tab | `8px`~`12px` |
| 输入框 | `8px` |
| 图片/附件 | `8px` |
| 骨架屏 | `4px` |

### 4.3 布局结构

```
┌─────────────────────────────────────────┐
│  Header (h-16)                          │  ← 固定顶栏：Logo + 搜索 + 设置
├────────┬────────────────────────────────┤
│        │                                │
│ Sidebar│         Main Content           │  ← 侧边栏(240px) + 可缩放主区
│ (240px)│         (flex-1)               │
│        │                                │
├────────┴────────────────────────────────┤
│  Mobile Bottom Bar (可选)               │  ← 移动端写信快捷操作
└─────────────────────────────────────────┘
```

- **侧边栏**：导航 + 账号列表 + 同步进度，固定宽度 240px
- **主区域**：邮件列表（左，最大 420px）+ 邮件详情（右，弹性）
- **移动端**：侧边栏全屏覆盖 + 邮件详情全屏滑入

---

## 五、组件设计规范

### 5.1 按钮

| 类型 | 样式 |
|------|------|
| 主操作（发送/确认） | 主色填充 + 白色文字 + `rounded-[12px]` |
| 次操作（存草稿/取消） | 灰色背景（`var(--muted)`）+ 次文字 |
| 图标按钮 | 无背景，悬停时显示浅色圆形背景 |
| 危险操作（删除） | 主色（红色）|
| 添加账号 | 金色虚线边框圆圈 + Plus 图标 |

### 5.2 卡片

- 背景：`var(--card)`
- 边框：`1px solid var(--card-border)`
- 圆角：`16px`
- 阴影：`0 2px 8px rgba(0,0,0,0.04)`
- 内边距：`24px`

### 5.3 导航与 Tab

- 侧边栏导航项：文字 + 图标，激活态白色背景反色文字
- NavSlider：弹簧动画滑块（framer-motion stiffness:400, damping:22），跟随激活项
- 设置页 Tab：小圆角按钮（`8px`），激活态主色填充

### 5.4 邮件列表项

- 未读：主题加粗 + 左侧蓝色圆点
- 已选：蓝色左边框 + 浅灰背景
- 时间格式：今天 `HH:mm` / 昨天 / N天前 / MM月DD日
- 密度：舒适（默认）/ 紧凑 两档

### 5.5 头像

- 圆形（`rounded-full`）
- 颜色来源优先级：`account.brand_color` → provider 品牌色 → hash 取色 → 默认紫 `#6366f1`
- 无头像时显示首字母或 User 图标

### 5.6 状态指示

| 状态 | 颜色 | 位置 |
|------|------|------|
| 同步中 | 金色旋转 | 侧边栏账号旁 |
| 同步正常 | 绿色圆点 | 侧边栏账号旁 |
| 同步异常 | 红色圆点 | 侧边栏账号旁 |
| 未读数量 | 红色圆形 badge | 导航项右上角 |
| 大附件 | 黄色警告标签 | 附件列表 |

### 5.7 空态与加载

- 骨架屏：shimmer 横向扫光动画（`linear-gradient` + `background-position` 动画）
- 空态：居中图标 + 提示文字
- 页面切换：从下方 12px 淡入（`pageSlideIn`）

---

## 六、动画与交互

### 6.1 页面过渡

- 入场：`translateY(12px) → 0` + `opacity: 0 → 1`，duration 0.35s，cubic-bezier(0.32, 0.72, 0, 1)
- 可通过设置关闭（`.no-animations` 类）

### 6.2 移动端详情

- 进入：从右侧滑入（`translateX(100%) → 0`）
- 退出：向右侧滑出（`0 → 100%`）

### 6.3 NavSlider

- 弹簧物理动画（framer-motion spring）
- 支持按下时的 jelly 收缩效果

### 6.4 数据刷新

- 刷新按钮旋转动画
- SSE 实时更新未读数（无闪烁，局部替换）

---

## 七、暗色模式策略

采用 `[data-theme="dark"]` + `.dark` 双重选择器保证兼容性。

切换逻辑：
1. 首屏 `<script>` 同步读取 `localStorage.theme` 设置 `data-theme` 属性（防闪白）
2. 用户切换后写入 localStorage 并更新属性
3. 所有颜色通过 CSS 变量控制，背景/文字/边框一一对应

Dark 模式特点：
- 背景偏暖棕而非纯黑，保持整体温度一致性
- 主色略微提亮（`#c43d3d → #d44638`）增强可读性
- 金色在暗色下更亮（`#c9a96e → #d4b87a`）

---

## 八、响应式断点

| 断点 | 行为 |
|------|------|
| `<640px`（手机） | 侧边栏隐藏，汉堡菜单触发覆盖层；邮件列表/详情串行显示 |
| `640px–1023px`（平板） | 侧边栏可收起；邮件列表(320px)+ 详情弹性 |
| `≥1024px`（桌面） | 固定侧边栏(240px)；邮件列表(420px)+ 详情弹性；三栏可选 |

---

## 九、设计语言总结

```
温暖米色基底  ×  朱红强调       =  东方现代主义邮件客户端
粗衬线标题    ×  细宋体正文     =  人文与技术并存
弹簧动画      ×  克制定时       =  灵动但不浮夸
品牌色头像    ×  统一圆角卡片   =  个性化与秩序平衡
```
